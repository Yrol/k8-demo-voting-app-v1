# Postgres pod
# Using the official Postgres image from Dockerhub
# Using the default Postgres port 5432

apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    name: postgres-pod
    app: demo-voting-app

spec:
  containers:
    - name: postgres-app
      image: postgres
      ports: 5432